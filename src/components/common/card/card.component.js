import React from 'react'
import './card.style.scss'

export default ({customClassName}) => {
    return (
        <div className={`card-container ${customClassName}`}>
            
        </div>
    )
}